// let userName = new String()
let userName = "deepak saini"
// userName = userName.length // => provide length of string including space

// userName = userName.charAt(2) // => provide character through the index value

// userName = userName.indexOf("p") // => provide index through the key value

// userName = userName.split(" ") // => convert into array :- ["deepak", "saini"]

// userName = userName.substring(0, 4) // => provide word index 0 to 4 : "deep"

// let userName = "  deepak saini  "
// userName = userName.trim() // =>  it remove white space from start and End 

// let userName = "deepak=saini"
// userName = userName.replace("=", " ") // =>  it replace the value 

console.log(typeof userName)
